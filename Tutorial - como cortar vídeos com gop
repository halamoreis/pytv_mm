Tutorial para cortar vídeos:

1. Abrir o arquivo resolucoes.py e alterar formato, resoluções e o código, em seguida executar pelo terminal com o comando:
    python resolucoes > resolucoes.sh
2. Para cada resolução gerada, abrir o arquivo ffprobe.sh e alterar nome e formato para o nome e o formato do arquivo correspondente e em seguida executar.
3. Abrir o arquivo metascript.py, alterar o nome para o da resolução correspondente, em seguida executar o arquivo metascript.py usando o comando:
     python metascript.sh > gop.sh
4. Executar o arquivo gop.sh

